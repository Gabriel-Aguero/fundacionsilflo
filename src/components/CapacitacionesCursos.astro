---
import db from "../components/DB/db.json";
import CapacitacionesItems from "./CapacitacionesItems.astro";

const cursos = db;
---

<div
  class="max-w-6xl flex flex-col items-center justify-center mt-32 mx-auto"
  id="capacitaciones"
>
  <div class="flex flex-col items-center justify-center gap-4 py-5">
    <h1 class="text-2xl text-[#10494d] font-bold text-balance">
      Fundación SILFLO te capacita
    </h1>
    <p
      class="sm:w-3/5 font-bold text-xl leading-relaxed sm:pl-10 pl-0 text-[#0b9096]"
    >
      El Sistema de Aula invertida de Fundación SILFLO, te va a convertir en un
      experto aprendiz del área que te estés formando.
    </p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    {cursos.map((curso) => <CapacitacionesItems {...curso} />)}
  </div>
</div>
